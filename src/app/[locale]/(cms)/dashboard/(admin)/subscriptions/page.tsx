"use client";

import React from "react";

import SubscriptionList from "@/views/AdminSubscriptions/SubscriptionList";

type Props = {};

const page = (props: Props) => {
  return <SubscriptionList />;
};

export default page;
